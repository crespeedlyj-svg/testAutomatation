// ==============================================================
// ACT — 유효이자율계산 통합 테스트 (act_0002M)
// 생성일시: 2026-07-01  |  파일: 20260701_act_0002M_inte.spec.ts
// 화면: 유효이자율계산  |  메뉴경로: 재무관리 > 기초정보관리 > 유효이자율계산
// crudType: CALC — 클라이언트 전용 계산 화면(유효이자율, Newton법).
//   서버 조회/저장 API 가 없어 UI-driven 조회 시나리오가 존재하지 않아
//   빈 describe 블록으로 생성한다.
// TODO: 실제 MENU_ID 확인 필요 — 00_menu_ids.json 캐시에 act_0002M 항목 없음.
//       (M_MIS_XX_XX_XX 플레이스홀더 상태)
// storageState 자동 주입 — 로그인/goto 코드 작성 절대 금지
// ==============================================================
import { test } from '../fixtures';

// ============================================================================
test.describe('유효이자율계산 통합 테스트', () => {

  // crudType=CALC (클라이언트 전용 계산 화면) — 서버 조회 API 가 없어 통합 시나리오 없음.
  // 계산 로직 UI 검증 시나리오가 확정되면 이 블록에 TC 를 추가한다.
  test.skip('[no:0] 유효이자율계산 - 서버 조회 없음 (CALC 화면, 통합 대상 없음)', async () => {
    // 의도적으로 스킵. act_0002M 은 클라이언트 계산 전용 화면.
  });

});
