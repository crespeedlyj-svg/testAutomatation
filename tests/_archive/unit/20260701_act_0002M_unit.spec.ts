// ==============================================================
// ACT — 유효이자율계산 단위 테스트 (act_0002M)
// 생성일시: 2026-07-01  |  파일: 20260701_act_0002M_unit.spec.ts
// 화면: 유효이자율계산
// crudType: CALC — 클라이언트 전용 계산 화면(유효이자율, Newton법)
//   gfn_tran(서버 호출) 없음 → API-direct 로 검증할 서버 시나리오가 없음.
//   따라서 단위 테스트 대상 시나리오가 존재하지 않아 빈 describe 블록으로 생성한다.
// storageState 자동 주입 — 로그인/goto/navigateTo 코드 작성 절대 금지
// ==============================================================
import { test } from '../fixtures';

// ============================================================================
test.describe('유효이자율계산 단위 테스트', () => {

  // crudType=CALC (클라이언트 전용 계산 화면) — 서버 API 호출이 없어 단위 시나리오 없음.
  // 시나리오가 확정되면 이 블록에 TC 를 추가한다.
  test.skip('[no:0] 유효이자율계산 - 서버 API 없음 (CALC 화면, 단위 대상 없음)', async () => {
    // 의도적으로 스킵. act_0002M 은 클라이언트 계산 전용 화면.
  });

});
